Never create code that is not requested.

Always explain architecture first.

Never install unnecessary packages.

Prefer composition over inheritance.

Use Prisma.

Use dependency injection correctly.

Never create overly complex abstractions.

Always ask before introducing Redis.

Keep folder structure clean.

Every implementation must compile.
